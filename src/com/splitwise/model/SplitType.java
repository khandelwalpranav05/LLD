package com.splitwise.model;

public enum SplitType {
    EQUAL,      // Split equally among all participants
    EXACT,      // Each person pays an exact amount
    PERCENTAGE  // Each person pays a percentage
}
